"""Scenario grids and report writers. Each experiment module exposes the grid
as data (scenarios, estimator specs), a run function, and a report writer, so
that tests import from the package rather than from a top-level script."""
